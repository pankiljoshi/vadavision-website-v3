import { Inter, Montserrat, Prompt, Raleway, Urbanist } from "next/font/google";
import "./globals.css";
import React from "react";

const inter = Inter({ subsets: ["latin"], variable: "--inter" });

const montserrat = Montserrat({
  subsets: ["latin"],
  variable: "--montserrat",
});

const prompt = Prompt({
  subsets: ["latin"],
  variable: "--prompt",
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
});

const raleway = Raleway({
  subsets: ["latin"],
  variable: "--raleway",
});

const urbanist = Urbanist({
  subsets: ["latin"],
  variable: "--urbanist",
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
});
export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

// eslint-disable-next-line react/prop-types
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={
          (inter.variable,
          montserrat.variable,
          prompt.variable,
          raleway.variable,
          urbanist.variable)
        }
      >
        {children}
      </body>
    </html>
  );
}
